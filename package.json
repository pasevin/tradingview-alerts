{
  "name": "tradingview-alerts",
  "version": "1.0.0",
  "private": true,
  "description": "Native macOS menu-bar app for TradingView alerts — Tauri 2 desktop + self-hostable relay. Open-core, no proprietary runtime.",
  "license": "AGPL-3.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "turbo run build",
    "type-check": "turbo run type-check",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "dev": "turbo run dev --parallel"
  },
  "devDependencies": {
    "@eslint/js": "^9",
    "eslint": "^9",
    "eslint-plugin-react-hooks": "^5",
    "globals": "^15",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8"
  }
}
